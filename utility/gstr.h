/*
* *gstr.h* 
* *level string*
*/
#pragma once

#define STRING_REFERENCE 010
#define STRING_LINE 020

#endif

#include <string>
#include <cstdio>
#include <math.h>

#define _C_ 8

struct REFERENCE{
const std::string local_reference(REFERENCE&)const;

std::string _Refer__to_ *localized_string;
std::FILE * GStr;

#define REFERENCE_LOCAL_STRING_SIZE 64x64

const char *REFERENCE::local_reference(reference_buffer(REFERENCE_LOCAL_STRING_SIZE))const;
char * _T[1024];

 std::size_t * localized_referencedSize(REFERENCE);

void copy_reference(REFERENCE reference){
  reference._Refer__to_.strcpy(char * _T, const char * gstring(REFERENCE*));
};

void reference_null(REFERENCE local_reference){
   local_reference.GStr.remove(std::FILE * GStr);
    if(local_reference.GStr.remove(std::FILE * GStr))
    {
      local_reference.GStr.fprintf("GString Reference Is Null, Error Code: 0xLocalString\n");
      GStr * gstr == NULL;
       size_t * GNull = NULL;
      
       return gstr;
        return GNull;
    };
};

REFERENCE * allocate_reference(){
   REFERENCE * reference = (REFERENCE*)malloc(sizeof(REFERENCE));
   return reference;
};

};
